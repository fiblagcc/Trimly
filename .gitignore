# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env — secrets live here, never commit
.env
.env.local
.env.*.local

# Build context / private project brief — not shipped to the public repo
docs/

# Vercel
.vercel

# Local AI-harness tooling (impeccable design skill, etc.) — workflow aid, not app code
.claude/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local AI-harness skill tooling (taste-skill, etc.)
.agents/

# Playwright screenshots (binary churn)
e2e/shots/
e2e/loop-login.mjs

skills-lock.json

# Native C# desktop app (local only, kept out of the public web repo)
/Trimly Desktop/

# === trimly-phone-app branch only: Android / Kotlin build artifacts ===
# (the web app ships from main; these keep Gradle/Studio junk out of the repo)
.gradle/
**/build/
.idea/
*.iml
captures/
.cxx/
local.properties
*.jks
*.keystore
