VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional — only for the DB tooling in scripts/ (applying migrations + the RLS
# check). A Supabase personal access token: https://supabase.com/dashboard/account/tokens
# Not used by the app itself. Keep it in .env (gitignored), never commit it.
# SUPABASE_ACCESS_TOKEN=
